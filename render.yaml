services:
  - type: web
    name: decoy-auction-cars
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    env: node
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
